/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"i.ibb.co",
			"i.pinimg.com",
			"upload.wikimedia.org",
			"assets.nflxext.com",
			"media.licdn.com",
		],
	},
};

export default nextConfig;
